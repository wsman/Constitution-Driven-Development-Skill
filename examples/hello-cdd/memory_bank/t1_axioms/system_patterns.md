# System Patterns (系统模式)

**项目**: hello-cdd  
**类型**: T1 公理层  
**用途**: 定义项目的目录结构和架构模式

---

## 📁 目录结构规范

```
hello-cdd/
├── memory_bank/           # Memory Bank (唯一真理源)
│   ├── t0_core/          # 核心层 - 身份和状态
│   ├── t1_axioms/        # 公理层 - 架构约束
│   ├── t2_protocols/     # 协议层 - 工作流程
│   └── t2_standards/     # 标准层 - 实现规范
├── specs/                # 特性规格 (由 cdd-feature.py 生成)
├── src/                  # 源代码
│   ├── __init__.py
│   └── hello.py          # 示例模块
└── tests/                # 测试代码
    ├── __init__.py
    └── test_hello.py     # 示例测试
```

---

## 🏗️ 架构模式

### 分层架构
| 层级 | 职责 | 文件位置 |
|------|------|----------|
| **T0 核心** | 身份、状态、导航 | `memory_bank/t0_core/` |
| **T1 公理** | 架构约束、技术栈 | `memory_bank/t1_axioms/` |
| **T2 标准** | 实现规范、工作流 | `memory_bank/t2_standards/` |

### 命名约定
- **文件**: 小写 + 下划线: `feature_specification.md`
- **目录**: 小写 + 连字符: `t0_core/`, `t1_axioms/`
- **模板**: 大写前缀: `DS-050_`, `WF-001_`

---

## ✅ Tier 1 验证标准

验证文件系统结构是否符合本文档定义：

```bash
# 验证目录存在
test -d memory_bank/t0_core
test -d memory_bank/t1_axioms
test -d src
test -d tests
```

---

**宪法依据**: §303